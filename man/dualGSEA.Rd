% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{dualGSEA}
\alias{dualGSEA}
\title{Reimplementation of dualGSEA (Bull et al., 2024) but defaults with
replaid backend. For the preranked test we still use fgsea. Should
be much faster than original using fgsea + GSVA::ssGSEA.}
\usage{
dualGSEA(
  X,
  y,
  gmt,
  matG = NULL,
  fc.test = "fgsea",
  gsea.method = "replaid.ssgsea"
)
}
\arguments{
\item{X}{Expression matrix with genes on rows and samples on columns}

\item{y}{Binary vector (0/1) indicating group membership}

\item{gmt}{List of gene sets in GMT format}

\item{matG}{Optional sparse matrix of gene sets (will be computed from gmt if NULL)}

\item{fc.test}{Method for fold change testing ("fgsea", "ztest", "rankcor", "cor")}

\item{gsea.method}{Method for single-sample enrichment ("replaid.ssgsea", "replaid.gsva", "ssgsea", "gsva")}
}
\value{
Data frame with results from dual testing including fold changes,
p-values, and combined statistical measures.
}
\description{
Reimplementation of dualGSEA (Bull et al., 2024) but defaults with
replaid backend. For the preranked test we still use fgsea. Should
be much faster than original using fgsea + GSVA::ssGSEA.
}
\examples{
\dontrun{
# Create example expression matrix
set.seed(123)
X <- matrix(rnorm(1000), nrow = 100, ncol = 20)
rownames(X) <- paste0("GENE", 1:100)
colnames(X) <- paste0("Sample", 1:20)

# Create binary group vector
y <- rep(c(0, 1), each = 10)

# Create example gene sets
gmt <- list(
  "Pathway1" = paste0("GENE", 1:20),
  "Pathway2" = paste0("GENE", 15:35),
  "Pathway3" = paste0("GENE", 30:50)
)

# Perform dualGSEA with fgsea (requires fgsea package)
results <- dualGSEA(X, y, gmt, fc.test = "fgsea", gsea.method = "replaid.ssgsea")
print(head(results))

# Perform dualGSEA with correlation test
results_cor <- dualGSEA(X, y, gmt, fc.test = "cor", gsea.method = "replaid.gsva")
print(head(results_cor))
}

}
